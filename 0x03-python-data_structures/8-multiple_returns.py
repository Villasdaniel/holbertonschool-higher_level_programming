#!/usr/bin/python3
def multiple_returns(sentence):
        if len(sentence) = 0:
                y = "None"
        else:
                y = sentence[0]
        x = len(sentence)
        tuple_a = (x, y)
        return(tuple_a)
